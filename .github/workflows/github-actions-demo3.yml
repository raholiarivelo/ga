name: GitHub Actions Demo 3
on: [push]
jobs:
  slackNotification:
    name: Slack Notification
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Slack Notification
      uses:  raholiarivelo/ga@v2
      env:
        SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}